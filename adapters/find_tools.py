# import asyncio,sys
# from mcp import ClientSession, StdioServerParameters
# from mcp.client.stdio import stdio_client


# tools = asyncio.run(find_file_tools())
# print(f'Discover {len(tools)}', file=sys.stderr)
# for t in tools:
#         print(f"- {t.name}:\n\t{t.description}", file=sys.stderr)